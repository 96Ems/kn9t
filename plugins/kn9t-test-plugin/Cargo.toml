[package]
name = "kn9t-test-plugin"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

# Test-only plugin binary. Configured entirely via env vars.
# GI-1: one workspace dep (kn9t-plugin-sdk).
[dependencies]
kn9t-plugin-sdk = { path = "../../kn9t-plugin-sdk" }
serde_json       = { workspace = true }
